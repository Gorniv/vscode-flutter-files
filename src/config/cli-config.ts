import { IConfig } from './../models/config';

export const config: IConfig = {
  appName: 'new_instagramm',
  apps: [
    {
      root: 'src',
    },
  ],
  defaults: {
    bloc: {
    },
    state: {
    },
  },
};
