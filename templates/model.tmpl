import 'package:equatable/equatable.dart';

/// generate by https://javiercbk.github.io/json_to_dart/
class Autogenerated${upperName} {
  List<${upperName}Model> results;

  Autogenerated${upperName}({this.results});

  Autogenerated${upperName}.fromJson(Map<String, dynamic> json) {
    if (json['results'] != null) {
      results = new List<${upperName}Model>();
      json['results'].forEach((v) {
        results.add(new ${upperName}Model.fromJson(v));
      });
    }
  }

  Map<String, dynamic> toJson() {
    final Map<String, dynamic> data = new Map<String, dynamic>();
    if (this.results != null) {
      data['results'] = this.results.map((v) => v.toJson()).toList();
    }
    return data;
  }
}

class ${upperName}Model extends Equatable {
  int id;
  String name;

  ${upperName}Model({this.id, this.name}) : super([id, name]);

  /// use : super([json]) for Equatable
  ${upperName}Model.fromJson(Map<String, dynamic> json) : super([json]) {
    id = json['id'];
    name = json['name'];
  }

  Map<String, dynamic> toJson() {
    final Map<String, dynamic> data = new Map<String, dynamic>();
    data['id'] = this.id;
    data['name'] = this.name;
    return data;
  }
}
