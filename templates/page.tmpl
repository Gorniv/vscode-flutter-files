import 'package:flutter/material.dart';
import 'package:${appName}/common/drawer.dart';
import 'package:${appName}/${inputName}/index.dart';

class ${upperName}Page extends StatelessWidget {
  static const String routeName = "/${privateName}";

  @override
  Widget build(BuildContext context) {
    var _${privateName}Bloc = new ${upperName}Bloc();
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("${upperName}"),
      ),
      body: new ${upperName}Screen(${privateName}Bloc: _${privateName}Bloc),
      endDrawer: getNavDrawer(context, ${upperName}Page.routeName),
    );
  }
}
