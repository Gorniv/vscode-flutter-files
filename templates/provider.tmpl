import 'package:${appName}/${inputName}/index.dart';

class ${upperName}Provider {
  final ${upperName}Repository _${upperName}Repository = new ${upperName}Repository();

  ${upperName}Provider();

  void test() {
    this._${upperName}Repository.test();
  }
}
